.TH _printf 3 "Holberton" "User Commands"
.SH NAME
_printf \- custom printf-like function
.SH SYNOPSIS
.B #include "main.h"
.br
.B int _printf(const char *format, ...);
.SH DESCRIPTION
The
.B _printf
function writes formatted output to the standard output stream.
This project subset supports the following conversion specifiers:
.BR %c ", " %s ", " %% ", " %d ", " %i .
.P
When the task specification does not define a behavior for an edge case,
mimic the behavior of
.BR printf (3)
for the supported subset.
.P
Edge cases implemented in this project:
.IP \[bu]
If
.I format
is
.BR NULL ,
.B _printf
returns \-1 and produces no output.
.IP \[bu]
If the format string ends with a lone percent sign (e.g. "%"),
.B _printf
returns \-1 and produces no output.
.SH RETURN VALUE
On success, returns the number of characters printed (excluding the
terminating null byte). On error, returns \-1.
.SH IMPLEMENTATION NOTES
No global variables. No more than 5 functions per file.
Output uses
.BR write (2).
Variadic arguments handled with
.BR stdarg.h
(va_list, va_start, va_arg, va_end).
.SH AUTHORS
Fahad Abdulaziz Alghamdi, Yasir Musaad Alshabanah
