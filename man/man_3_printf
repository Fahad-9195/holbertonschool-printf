.TH _printf 3 "Holberton" "User Commands"
.SH NAME
_printf \- custom printf-like function
.SH SYNOPSIS
.B #include "main.h"
.br
.B int _printf(const char *format, ...);
.SH DESCRIPTION
The
.B _printf
function writes formatted output to the standard output stream.
Supported conversion specifiers:
.BR %c ", " %s ", " %% ", " %d ", " %i .
.P
If
.I format
is
.BR NULL ,
.B _printf
returns -1 and produces no output.
If the format string ends with a lone percent sign (e.g. "%"),
.B _printf
returns -1 and produces no output.
.P
Unknown specifier prints the percent sign then the character literally.
.SH RETURN VALUE
Returns the number of characters printed (excluding the terminating null byte). On error, returns -1.
.SH IMPLEMENTATION NOTES
Output uses
.BR write (2).
Variadic arguments handled with
.BR stdarg.h
(va_list, va_start, va_arg, va_end). No globals; ≤ 5 functions per file.
.SH EXAMPLES
.nf
_printf("Hello %s %d%%\\n", "world", 100);
.fi
.SH SEE ALSO
.BR printf (3),
.BR write (2)
.SH AUTHORS
Fahad Abdulaziz Alghamdi, Yasir Musaad Alshabanah
